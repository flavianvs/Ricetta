import { FiltraRicetteDirective } from './filtra-ricette.directive';

describe('FiltraRicetteDirective', () => {
  it('should create an instance', () => {
    const directive = new FiltraRicetteDirective();
    expect(directive).toBeTruthy();
  });
});
